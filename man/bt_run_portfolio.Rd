% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/native_engine.R
\name{bt_run_portfolio}
\alias{bt_run_portfolio}
\title{Run a native shared-capital multi-instrument backtest}
\usage{
bt_run_portfolio(
  tickers,
  data = NULL,
  strategy = bt_strategy_spec("donchian"),
  risk = NULL,
  ps_value = NULL,
  ps_type = NULL,
  initial_equity = 1e+05,
  reinvest = TRUE,
  execution = bt_execution_spec(execution = "breakout"),
  start_date = "1900-01-01",
  end_date = Sys.Date(),
  normalize_risk = NULL,
  geometric = TRUE,
  only_returns = FALSE,
  verbose = FALSE,
  clean_di = TRUE,
  max_positions = Inf,
  priority = c("order")
)
}
\arguments{
\item{tickers}{Character vector, \code{xts} object, or named list of character/\code{xts}
instruments.}

\item{data}{Optional \code{xts} object or named list of preloaded \code{xts} objects.}

\item{strategy}{A \code{bt_strategy_spec}, character strategy type, or named list
of per-instrument strategy specs.}

\item{risk}{Optional \code{bt_risk_spec}, or named list of per-instrument specs. If
\code{NULL}, each instrument resolves \code{ps_value}/\code{ps_type} from arguments or
metadata.}

\item{ps_value, ps_type}{Position-sizing value and type used when \code{risk} is
\code{NULL}. Scalars apply to every instrument; named vectors/lists can override
by instrument label or source symbol.}

\item{initial_equity}{Starting shared account equity.}

\item{reinvest}{Whether sizing uses current shared equity. Can be scalar or
named per instrument when \code{risk} is \code{NULL}.}

\item{execution}{A \code{bt_execution_spec}, or named list of per-instrument specs.}

\item{start_date, end_date}{Optional date bounds used when fetching/subsetting.}

\item{normalize_risk}{Optional annualized risk target in percent for the
portfolio return stream.}

\item{geometric}{Kept for wrapper compatibility.}

\item{only_returns}{If \code{TRUE}, return only the portfolio \code{xts} returns object.}

\item{verbose}{If \code{TRUE}, print a compact execution summary.}

\item{clean_di}{If \code{TRUE}, apply DI cleanup before running.}

\item{max_positions}{Maximum simultaneous open instruments.}

\item{priority}{Entry priority. Currently only \code{"order"} is supported.}
}
\value{
A \code{bt_portfolio_result} list, or an \code{xts} returns object when
\code{only_returns = TRUE}.
}
\description{
This runner uses one account equity curve for all instruments. Each instrument
has its own strategy, risk, execution, metadata, signals, and positions, while
later entries size from the shared account equity. Quantity calculation still
delegates to \code{positionsizer}; \code{backtestforge} owns the shared-capital event
loop and trade ledger.
}
\details{
\code{bt_run_portfolio()} simulates all instruments on one shared equity curve.
This is different from \code{bt_batch(gen_portfolio = ...)}, which aggregates
already-finished individual return streams.

Per-instrument names are matched against the list label, source symbol,
ticker argument, and nested metadata such as \code{attr(x, "information")$ticker}.
Direct \code{xts} inputs resolve their source symbol from \code{attr(x, "symbol")},
then \code{attr(x, "ticker")}, then \code{attr(x, "information")$ticker}, then the
list label.

Current portfolio execution support is limited to \code{"breakout"} and
\code{"same_close"}. Portfolio pyramiding, \verb{next_*} execution modes, margin, and
advanced entry-priority rules are not implemented yet.
}
\examples{
\dontrun{
res <- bt_run_portfolio(
  tickers = list(WDO = wdo_xts, WIN = win_xts),
  strategy = bt_strategy_spec("donchian", up = 40, down = 20),
  ps_value = c(WDO = 1, WIN = 2),
  ps_type = c(WDO = "contract", WIN = "contract"),
  execution = bt_execution_spec(
    execution = "breakout",
    fee_value = 0,
    fee_type = "contract",
    slip_value = 0,
    slip_type = "cash"
  ),
  initial_equity = 100000
)
}
}
