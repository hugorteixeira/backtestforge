% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{eldoc}
\alias{eldoc}
\title{Donchian channels helper and chart (ElDoc)}
\usage{
eldoc(
  ticker,
  name_to_use = NULL,
  x = 25,
  y = 25,
  hi.col = "High",
  lo.col = "Low",
  type = "chart"
)
}
\arguments{
\item{ticker}{An \code{xts} OHLC object.}

\item{name_to_use}{Optional character label for the chart title; defaults to
the symbol name derived from \code{ticker}.}

\item{x}{Integer window for the rolling maximum (upper channel).}

\item{y}{Integer window for the rolling minimum (lower channel).}

\item{hi.col}{Column name to use as High (default "High").}

\item{lo.col}{Column name to use as Low (default "Low").}

\item{type}{One of \code{"chart"} (default), \code{"data"}, or \code{"fulldata"}.
\itemize{
\item \code{"chart"}: draws a chart and returns invisibly.
\item \code{"data"}: returns an \code{xts} with columns \code{X} (upper) and \code{Y} (lower), lagged by one bar.
\item \code{"fulldata"}: returns OHLC columns plus \code{X} and \code{Y}.
}}
}
\value{
Depending on \code{type}: draws a chart (invisible return), or returns an
\code{xts} object with channel values.
}
\description{
Computes upper/lower Donchian channel lines using rolling max/min and can
either plot them on top of the input OHLC series or return them as data.
}
\examples{
# eldoc(my_xts_ohlc, x = 20, y = 20, type = "data")
}
