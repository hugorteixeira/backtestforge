% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/native_engine.R
\name{bt_run_native}
\alias{bt_run_native}
\title{Run a native in-memory backtest}
\usage{
bt_run_native(
  ticker,
  data = NULL,
  strategy = bt_strategy_spec("donchian"),
  risk = NULL,
  ps_value = NULL,
  ps_type = NULL,
  initial_equity = 1e+05,
  reinvest = TRUE,
  execution = bt_execution_spec(),
  max_leverage = NULL,
  integer_qty = NULL,
  start_date = "1900-01-01",
  end_date = Sys.Date(),
  normalize_risk = NULL,
  geometric = TRUE,
  only_returns = FALSE,
  verbose = FALSE,
  clean_di = TRUE,
  funding = NULL,
  execution_data = NULL,
  report = TRUE,
  show_quarterly = FALSE,
  research_blocks = TRUE
)
}
\arguments{
\item{ticker}{Character symbol or \code{xts} OHLC object.}

\item{data}{Optional preloaded \code{xts} object. Used when \code{ticker} is a label.}

\item{strategy}{A \code{bt_strategy_spec}, or a character strategy type.}

\item{risk}{Optional \code{bt_risk_spec}. If \code{NULL}, \code{ps_value}/\code{ps_type} are
resolved from arguments or ticker metadata.}

\item{ps_value, ps_type}{Position-sizing value and type used when \code{risk} is
\code{NULL}.}

\item{initial_equity}{Starting account equity when \code{risk} is \code{NULL}.}

\item{reinvest}{Whether risk sizing uses current equity when \code{risk} is \code{NULL}.}

\item{execution}{A \code{bt_execution_spec}.}

\item{max_leverage}{Optional notional cap as a multiple of equity when \code{risk}
is \code{NULL}.}

\item{integer_qty}{Optional quantity rounding override when \code{risk} is \code{NULL}.
The default is integer quantities except for Binance-style USDT-M crypto
futures or metadata with sub-unit quantity steps.}

\item{start_date, end_date}{Optional date bounds used when fetching/subsetting.}

\item{normalize_risk}{Optional annualized risk target in percent.}

\item{geometric}{Kept for wrapper compatibility.}

\item{only_returns}{If \code{TRUE}, return only the \code{xts} returns object.}

\item{verbose}{If \code{TRUE}, print detailed stats and transactions.}

\item{clean_di}{If \code{TRUE}, apply DI cleanup before running.}

\item{funding}{Optional funding events for perpetual futures. \code{TRUE} reads
funding columns from \code{data} or fetches
\code{finharvest::finget_binance_fut_funding()} only for explicit
\verb{_PERPETUAL} symbols; Binance \code{QTR}/\code{NQTR}/dated delivery futures keep zero
funding unless an explicit \code{xts}/\code{data.frame} is supplied. Explicit
funding data should include \code{date}/\code{FundingRate} or
\code{timestamp}/\code{funding_rate} events.}

\item{execution_data}{Optional explicit lower/equal-timeframe OHLC \code{xts}
used when \code{execution$execution_timeframe} is not \code{"same"}. Character
tickers are fetched from \code{finharvest} automatically when omitted.}

\item{report}{If \code{TRUE}, print the standard console performance tables.}

\item{show_quarterly}{If \code{TRUE}, print quarterly return and net-profit
tables in the console report.}

\item{research_blocks}{If \code{TRUE}, include experimental diagnostic blocks
such as excursion thresholds in default reports and \code{bt_stats()}.}
}
\value{
A \code{bt_native_result} list, or an \code{xts} returns object when
\code{only_returns = TRUE}. In \code{trades}, \code{fees} is commission only,
\code{slippage} is slippage cost, and \code{total_cost} is their sum.
\code{trade_audit} expands completed trade episodes into order-level rows with
signal price, slippage-adjusted fill price, costs, bars held, and funding
summaries when funding applies.
When \code{normalize_risk} is supplied, \code{rets}, \code{stats}, and report performance
blocks use the risk-normalized return stream; \code{raw_rets}, \code{raw_stats},
trades, positions, and cost blocks keep the unnormalised execution path.
}
\description{
The native engine computes indicators, signals, stops, execution prices, and
cost ledgers in this package. Quantity calculation is delegated to
\code{positionsizer} from the resolved risk spec and instrument metadata.
}
